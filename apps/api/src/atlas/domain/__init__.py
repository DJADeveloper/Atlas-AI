"""Domain layer: the enterprise core (spine §5).

Pure Python — no frameworks, no SDKs, no I/O. The import-linter
contracts in `pyproject.toml` enforce this mechanically from M02 on.
Entities, value objects, domain events, and ports land here starting
at M03 (knowledge context skeleton).
"""
