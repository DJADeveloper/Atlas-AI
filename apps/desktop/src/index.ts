/**
 * Placeholder module for @atlas/desktop.
 *
 * The Tauri 2 shell lands at M14; until then this package exists so the pnpm workspace graph,
 * typecheck, and test pipeline exercise every package from M01 onward.
 */
export const PACKAGE_NAME = "@atlas/desktop";
