{
  "name": "@atlas/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Atlas web UI. Placeholder until M09 — Web UI chat + citations.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
