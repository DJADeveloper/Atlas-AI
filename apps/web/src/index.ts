/**
 * Placeholder module for the Atlas web UI.
 *
 * The Next.js application lands at M09 (Web UI chat + citations); until then
 * this package exists so the pnpm workspace graph, typecheck, and test
 * pipeline exercise every package from M01 onward.
 */
export const PACKAGE_NAME = "@atlas/web";
